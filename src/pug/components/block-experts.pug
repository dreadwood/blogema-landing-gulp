include ../components/card-expert

mixin block-experts({ className, color })
  section.b-experts(class=className)
    include ../mocks/experts
    -
      let experts = [
        ...expertsMock,
        ...expertsMock,
        ...expertsMock,
        ...expertsMock
      ]
    h2.b-experts__title Эксперты
    .b-experts__list
      each it in experts
        +card-expert({ it, color })
